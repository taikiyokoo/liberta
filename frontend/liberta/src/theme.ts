import { createTheme } from "@mui/material/styles";
import {  teal ,deepOrange,amber} from "@mui/material/colors";

const theme = createTheme({
  palette: {
    primary: teal,
    secondary: {
      main: "#9e9d24",
    },
  }
});

export default theme;
